# Auto detect text files and normalize line endings
* text=auto

# Shell scripts must use LF line endings (for Linux server)
*.sh text eol=lf
deploy/*.sh text eol=lf

# Python files
*.py text diff=python

# Ensure JSON and YAML use LF
*.json text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
*.toml text eol=lf

# Systemd service files
*.service text eol=lf

# Keep Windows line endings for Windows-specific files
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
