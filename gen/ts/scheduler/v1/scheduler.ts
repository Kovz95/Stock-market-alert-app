// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: scheduler/v1/scheduler.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Timestamp } from "../../google/protobuf/timestamp";

export const protobufPackage = "stockalert.scheduler.v1";

export interface GetSchedulerStatusRequest {
}

export interface CurrentJob {
  exchange: string;
  timeframe: string;
  started?: Date | undefined;
}

export interface LastRun {
  exchange: string;
  timeframe: string;
  completedAt?: Date | undefined;
}

export interface LastError {
  exchange: string;
  timeframe: string;
  message: string;
  time?: Date | undefined;
}

/** QueueBreakdown is the per-state task count from Asynq (default queue). */
export interface QueueBreakdown {
  /** waiting to be processed */
  pending: number;
  /** scheduled for a future time (e.g. daily at market close) */
  scheduled: number;
  /** currently being processed */
  active: number;
  /** failed, waiting to retry */
  retry: number;
  /** archived after max retries */
  archived: number;
}

export interface GetSchedulerStatusResponse {
  status: string;
  heartbeat?: Date | undefined;
  currentJob?: CurrentJob | undefined;
  lastRun?: LastRun | undefined;
  lastError?:
    | LastError
    | undefined;
  /** queue_size: total tasks in queue (pending + scheduled + active + retry + archived) */
  queueSize: number;
  /** active_workers: number of tasks currently being processed across all workers */
  activeWorkers: number;
  /** worker_processes: number of live asynq worker processes connected to Redis */
  workerProcesses: number;
  /** Breakdown by state so UI can show e.g. "124 (80 scheduled, 44 pending)" */
  queueBreakdown?: QueueBreakdown | undefined;
}

export interface GetExchangeScheduleRequest {
  /** "daily", "weekly", or "hourly" — defaults to "daily" if empty */
  timeframe: string;
}

export interface ExchangeScheduleRow {
  exchange: string;
  symbol: string;
  region: string;
  runTimeEt: string;
  runTimeUtc: string;
  localClose: string;
  localTz: string;
  timeRemainingSeconds: number;
  lastRunDate: string;
  lastRunStart: string;
  lastRunEnd: string;
}

export interface GetExchangeScheduleResponse {
  rows: ExchangeScheduleRow[];
}

export interface StartSchedulerRequest {
}

export interface StartSchedulerResponse {
  success: boolean;
  message: string;
}

export interface StopSchedulerRequest {
}

export interface StopSchedulerResponse {
  success: boolean;
  message: string;
}

export interface RunExchangeJobRequest {
  exchange: string;
  /** "daily", "weekly", "hourly" */
  timeframe: string;
}

export interface RunExchangeJobResponse {
  success: boolean;
  message: string;
}

export interface ListQueueTasksRequest {
  /** Queue name; empty means "default". */
  queue: string;
}

export interface QueueTask {
  /** "scheduled", "pending", "active" */
  state: string;
  /** task type e.g. "task:daily" */
  type: string;
  exchange: string;
  /** "daily", "weekly", "hourly" */
  timeframe: string;
  nextProcessAt?: Date | undefined;
  id: string;
}

export interface ListQueueTasksResponse {
  tasks: QueueTask[];
}

function createBaseGetSchedulerStatusRequest(): GetSchedulerStatusRequest {
  return {};
}

export const GetSchedulerStatusRequest: MessageFns<GetSchedulerStatusRequest> = {
  encode(_: GetSchedulerStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSchedulerStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSchedulerStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetSchedulerStatusRequest {
    return {};
  },

  toJSON(_: GetSchedulerStatusRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSchedulerStatusRequest>, I>>(base?: I): GetSchedulerStatusRequest {
    return GetSchedulerStatusRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSchedulerStatusRequest>, I>>(_: I): GetSchedulerStatusRequest {
    const message = createBaseGetSchedulerStatusRequest();
    return message;
  },
};

function createBaseCurrentJob(): CurrentJob {
  return { exchange: "", timeframe: "", started: undefined };
}

export const CurrentJob: MessageFns<CurrentJob> = {
  encode(message: CurrentJob, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exchange !== "") {
      writer.uint32(10).string(message.exchange);
    }
    if (message.timeframe !== "") {
      writer.uint32(18).string(message.timeframe);
    }
    if (message.started !== undefined) {
      Timestamp.encode(toTimestamp(message.started), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CurrentJob {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCurrentJob();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.exchange = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timeframe = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.started = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CurrentJob {
    return {
      exchange: isSet(object.exchange) ? globalThis.String(object.exchange) : "",
      timeframe: isSet(object.timeframe) ? globalThis.String(object.timeframe) : "",
      started: isSet(object.started) ? fromJsonTimestamp(object.started) : undefined,
    };
  },

  toJSON(message: CurrentJob): unknown {
    const obj: any = {};
    if (message.exchange !== "") {
      obj.exchange = message.exchange;
    }
    if (message.timeframe !== "") {
      obj.timeframe = message.timeframe;
    }
    if (message.started !== undefined) {
      obj.started = message.started.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CurrentJob>, I>>(base?: I): CurrentJob {
    return CurrentJob.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CurrentJob>, I>>(object: I): CurrentJob {
    const message = createBaseCurrentJob();
    message.exchange = object.exchange ?? "";
    message.timeframe = object.timeframe ?? "";
    message.started = object.started ?? undefined;
    return message;
  },
};

function createBaseLastRun(): LastRun {
  return { exchange: "", timeframe: "", completedAt: undefined };
}

export const LastRun: MessageFns<LastRun> = {
  encode(message: LastRun, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exchange !== "") {
      writer.uint32(10).string(message.exchange);
    }
    if (message.timeframe !== "") {
      writer.uint32(18).string(message.timeframe);
    }
    if (message.completedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.completedAt), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LastRun {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLastRun();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.exchange = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timeframe = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.completedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LastRun {
    return {
      exchange: isSet(object.exchange) ? globalThis.String(object.exchange) : "",
      timeframe: isSet(object.timeframe) ? globalThis.String(object.timeframe) : "",
      completedAt: isSet(object.completedAt)
        ? fromJsonTimestamp(object.completedAt)
        : isSet(object.completed_at)
        ? fromJsonTimestamp(object.completed_at)
        : undefined,
    };
  },

  toJSON(message: LastRun): unknown {
    const obj: any = {};
    if (message.exchange !== "") {
      obj.exchange = message.exchange;
    }
    if (message.timeframe !== "") {
      obj.timeframe = message.timeframe;
    }
    if (message.completedAt !== undefined) {
      obj.completedAt = message.completedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LastRun>, I>>(base?: I): LastRun {
    return LastRun.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LastRun>, I>>(object: I): LastRun {
    const message = createBaseLastRun();
    message.exchange = object.exchange ?? "";
    message.timeframe = object.timeframe ?? "";
    message.completedAt = object.completedAt ?? undefined;
    return message;
  },
};

function createBaseLastError(): LastError {
  return { exchange: "", timeframe: "", message: "", time: undefined };
}

export const LastError: MessageFns<LastError> = {
  encode(message: LastError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exchange !== "") {
      writer.uint32(10).string(message.exchange);
    }
    if (message.timeframe !== "") {
      writer.uint32(18).string(message.timeframe);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    if (message.time !== undefined) {
      Timestamp.encode(toTimestamp(message.time), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LastError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLastError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.exchange = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timeframe = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.time = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LastError {
    return {
      exchange: isSet(object.exchange) ? globalThis.String(object.exchange) : "",
      timeframe: isSet(object.timeframe) ? globalThis.String(object.timeframe) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      time: isSet(object.time) ? fromJsonTimestamp(object.time) : undefined,
    };
  },

  toJSON(message: LastError): unknown {
    const obj: any = {};
    if (message.exchange !== "") {
      obj.exchange = message.exchange;
    }
    if (message.timeframe !== "") {
      obj.timeframe = message.timeframe;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.time !== undefined) {
      obj.time = message.time.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LastError>, I>>(base?: I): LastError {
    return LastError.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LastError>, I>>(object: I): LastError {
    const message = createBaseLastError();
    message.exchange = object.exchange ?? "";
    message.timeframe = object.timeframe ?? "";
    message.message = object.message ?? "";
    message.time = object.time ?? undefined;
    return message;
  },
};

function createBaseQueueBreakdown(): QueueBreakdown {
  return { pending: 0, scheduled: 0, active: 0, retry: 0, archived: 0 };
}

export const QueueBreakdown: MessageFns<QueueBreakdown> = {
  encode(message: QueueBreakdown, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pending !== 0) {
      writer.uint32(8).int32(message.pending);
    }
    if (message.scheduled !== 0) {
      writer.uint32(16).int32(message.scheduled);
    }
    if (message.active !== 0) {
      writer.uint32(24).int32(message.active);
    }
    if (message.retry !== 0) {
      writer.uint32(32).int32(message.retry);
    }
    if (message.archived !== 0) {
      writer.uint32(40).int32(message.archived);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueueBreakdown {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueueBreakdown();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pending = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.scheduled = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.active = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.retry = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.archived = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueueBreakdown {
    return {
      pending: isSet(object.pending) ? globalThis.Number(object.pending) : 0,
      scheduled: isSet(object.scheduled) ? globalThis.Number(object.scheduled) : 0,
      active: isSet(object.active) ? globalThis.Number(object.active) : 0,
      retry: isSet(object.retry) ? globalThis.Number(object.retry) : 0,
      archived: isSet(object.archived) ? globalThis.Number(object.archived) : 0,
    };
  },

  toJSON(message: QueueBreakdown): unknown {
    const obj: any = {};
    if (message.pending !== 0) {
      obj.pending = Math.round(message.pending);
    }
    if (message.scheduled !== 0) {
      obj.scheduled = Math.round(message.scheduled);
    }
    if (message.active !== 0) {
      obj.active = Math.round(message.active);
    }
    if (message.retry !== 0) {
      obj.retry = Math.round(message.retry);
    }
    if (message.archived !== 0) {
      obj.archived = Math.round(message.archived);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueueBreakdown>, I>>(base?: I): QueueBreakdown {
    return QueueBreakdown.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueueBreakdown>, I>>(object: I): QueueBreakdown {
    const message = createBaseQueueBreakdown();
    message.pending = object.pending ?? 0;
    message.scheduled = object.scheduled ?? 0;
    message.active = object.active ?? 0;
    message.retry = object.retry ?? 0;
    message.archived = object.archived ?? 0;
    return message;
  },
};

function createBaseGetSchedulerStatusResponse(): GetSchedulerStatusResponse {
  return {
    status: "",
    heartbeat: undefined,
    currentJob: undefined,
    lastRun: undefined,
    lastError: undefined,
    queueSize: 0,
    activeWorkers: 0,
    workerProcesses: 0,
    queueBreakdown: undefined,
  };
}

export const GetSchedulerStatusResponse: MessageFns<GetSchedulerStatusResponse> = {
  encode(message: GetSchedulerStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    if (message.heartbeat !== undefined) {
      Timestamp.encode(toTimestamp(message.heartbeat), writer.uint32(18).fork()).join();
    }
    if (message.currentJob !== undefined) {
      CurrentJob.encode(message.currentJob, writer.uint32(26).fork()).join();
    }
    if (message.lastRun !== undefined) {
      LastRun.encode(message.lastRun, writer.uint32(34).fork()).join();
    }
    if (message.lastError !== undefined) {
      LastError.encode(message.lastError, writer.uint32(42).fork()).join();
    }
    if (message.queueSize !== 0) {
      writer.uint32(48).int32(message.queueSize);
    }
    if (message.activeWorkers !== 0) {
      writer.uint32(56).int32(message.activeWorkers);
    }
    if (message.workerProcesses !== 0) {
      writer.uint32(64).int32(message.workerProcesses);
    }
    if (message.queueBreakdown !== undefined) {
      QueueBreakdown.encode(message.queueBreakdown, writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSchedulerStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSchedulerStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.heartbeat = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.currentJob = CurrentJob.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.lastRun = LastRun.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.lastError = LastError.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.queueSize = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.activeWorkers = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.workerProcesses = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.queueBreakdown = QueueBreakdown.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSchedulerStatusResponse {
    return {
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      heartbeat: isSet(object.heartbeat) ? fromJsonTimestamp(object.heartbeat) : undefined,
      currentJob: isSet(object.currentJob)
        ? CurrentJob.fromJSON(object.currentJob)
        : isSet(object.current_job)
        ? CurrentJob.fromJSON(object.current_job)
        : undefined,
      lastRun: isSet(object.lastRun)
        ? LastRun.fromJSON(object.lastRun)
        : isSet(object.last_run)
        ? LastRun.fromJSON(object.last_run)
        : undefined,
      lastError: isSet(object.lastError)
        ? LastError.fromJSON(object.lastError)
        : isSet(object.last_error)
        ? LastError.fromJSON(object.last_error)
        : undefined,
      queueSize: isSet(object.queueSize)
        ? globalThis.Number(object.queueSize)
        : isSet(object.queue_size)
        ? globalThis.Number(object.queue_size)
        : 0,
      activeWorkers: isSet(object.activeWorkers)
        ? globalThis.Number(object.activeWorkers)
        : isSet(object.active_workers)
        ? globalThis.Number(object.active_workers)
        : 0,
      workerProcesses: isSet(object.workerProcesses)
        ? globalThis.Number(object.workerProcesses)
        : isSet(object.worker_processes)
        ? globalThis.Number(object.worker_processes)
        : 0,
      queueBreakdown: isSet(object.queueBreakdown)
        ? QueueBreakdown.fromJSON(object.queueBreakdown)
        : isSet(object.queue_breakdown)
        ? QueueBreakdown.fromJSON(object.queue_breakdown)
        : undefined,
    };
  },

  toJSON(message: GetSchedulerStatusResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.heartbeat !== undefined) {
      obj.heartbeat = message.heartbeat.toISOString();
    }
    if (message.currentJob !== undefined) {
      obj.currentJob = CurrentJob.toJSON(message.currentJob);
    }
    if (message.lastRun !== undefined) {
      obj.lastRun = LastRun.toJSON(message.lastRun);
    }
    if (message.lastError !== undefined) {
      obj.lastError = LastError.toJSON(message.lastError);
    }
    if (message.queueSize !== 0) {
      obj.queueSize = Math.round(message.queueSize);
    }
    if (message.activeWorkers !== 0) {
      obj.activeWorkers = Math.round(message.activeWorkers);
    }
    if (message.workerProcesses !== 0) {
      obj.workerProcesses = Math.round(message.workerProcesses);
    }
    if (message.queueBreakdown !== undefined) {
      obj.queueBreakdown = QueueBreakdown.toJSON(message.queueBreakdown);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSchedulerStatusResponse>, I>>(base?: I): GetSchedulerStatusResponse {
    return GetSchedulerStatusResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSchedulerStatusResponse>, I>>(object: I): GetSchedulerStatusResponse {
    const message = createBaseGetSchedulerStatusResponse();
    message.status = object.status ?? "";
    message.heartbeat = object.heartbeat ?? undefined;
    message.currentJob = (object.currentJob !== undefined && object.currentJob !== null)
      ? CurrentJob.fromPartial(object.currentJob)
      : undefined;
    message.lastRun = (object.lastRun !== undefined && object.lastRun !== null)
      ? LastRun.fromPartial(object.lastRun)
      : undefined;
    message.lastError = (object.lastError !== undefined && object.lastError !== null)
      ? LastError.fromPartial(object.lastError)
      : undefined;
    message.queueSize = object.queueSize ?? 0;
    message.activeWorkers = object.activeWorkers ?? 0;
    message.workerProcesses = object.workerProcesses ?? 0;
    message.queueBreakdown = (object.queueBreakdown !== undefined && object.queueBreakdown !== null)
      ? QueueBreakdown.fromPartial(object.queueBreakdown)
      : undefined;
    return message;
  },
};

function createBaseGetExchangeScheduleRequest(): GetExchangeScheduleRequest {
  return { timeframe: "" };
}

export const GetExchangeScheduleRequest: MessageFns<GetExchangeScheduleRequest> = {
  encode(message: GetExchangeScheduleRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timeframe !== "") {
      writer.uint32(10).string(message.timeframe);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetExchangeScheduleRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetExchangeScheduleRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.timeframe = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetExchangeScheduleRequest {
    return { timeframe: isSet(object.timeframe) ? globalThis.String(object.timeframe) : "" };
  },

  toJSON(message: GetExchangeScheduleRequest): unknown {
    const obj: any = {};
    if (message.timeframe !== "") {
      obj.timeframe = message.timeframe;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetExchangeScheduleRequest>, I>>(base?: I): GetExchangeScheduleRequest {
    return GetExchangeScheduleRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetExchangeScheduleRequest>, I>>(object: I): GetExchangeScheduleRequest {
    const message = createBaseGetExchangeScheduleRequest();
    message.timeframe = object.timeframe ?? "";
    return message;
  },
};

function createBaseExchangeScheduleRow(): ExchangeScheduleRow {
  return {
    exchange: "",
    symbol: "",
    region: "",
    runTimeEt: "",
    runTimeUtc: "",
    localClose: "",
    localTz: "",
    timeRemainingSeconds: 0,
    lastRunDate: "",
    lastRunStart: "",
    lastRunEnd: "",
  };
}

export const ExchangeScheduleRow: MessageFns<ExchangeScheduleRow> = {
  encode(message: ExchangeScheduleRow, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exchange !== "") {
      writer.uint32(10).string(message.exchange);
    }
    if (message.symbol !== "") {
      writer.uint32(18).string(message.symbol);
    }
    if (message.region !== "") {
      writer.uint32(26).string(message.region);
    }
    if (message.runTimeEt !== "") {
      writer.uint32(34).string(message.runTimeEt);
    }
    if (message.runTimeUtc !== "") {
      writer.uint32(42).string(message.runTimeUtc);
    }
    if (message.localClose !== "") {
      writer.uint32(50).string(message.localClose);
    }
    if (message.localTz !== "") {
      writer.uint32(58).string(message.localTz);
    }
    if (message.timeRemainingSeconds !== 0) {
      writer.uint32(64).int64(message.timeRemainingSeconds);
    }
    if (message.lastRunDate !== "") {
      writer.uint32(74).string(message.lastRunDate);
    }
    if (message.lastRunStart !== "") {
      writer.uint32(82).string(message.lastRunStart);
    }
    if (message.lastRunEnd !== "") {
      writer.uint32(90).string(message.lastRunEnd);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExchangeScheduleRow {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExchangeScheduleRow();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.exchange = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.symbol = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.region = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.runTimeEt = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.runTimeUtc = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.localClose = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.localTz = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.timeRemainingSeconds = longToNumber(reader.int64());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.lastRunDate = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.lastRunStart = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.lastRunEnd = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExchangeScheduleRow {
    return {
      exchange: isSet(object.exchange) ? globalThis.String(object.exchange) : "",
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
      region: isSet(object.region) ? globalThis.String(object.region) : "",
      runTimeEt: isSet(object.runTimeEt)
        ? globalThis.String(object.runTimeEt)
        : isSet(object.run_time_et)
        ? globalThis.String(object.run_time_et)
        : "",
      runTimeUtc: isSet(object.runTimeUtc)
        ? globalThis.String(object.runTimeUtc)
        : isSet(object.run_time_utc)
        ? globalThis.String(object.run_time_utc)
        : "",
      localClose: isSet(object.localClose)
        ? globalThis.String(object.localClose)
        : isSet(object.local_close)
        ? globalThis.String(object.local_close)
        : "",
      localTz: isSet(object.localTz)
        ? globalThis.String(object.localTz)
        : isSet(object.local_tz)
        ? globalThis.String(object.local_tz)
        : "",
      timeRemainingSeconds: isSet(object.timeRemainingSeconds)
        ? globalThis.Number(object.timeRemainingSeconds)
        : isSet(object.time_remaining_seconds)
        ? globalThis.Number(object.time_remaining_seconds)
        : 0,
      lastRunDate: isSet(object.lastRunDate)
        ? globalThis.String(object.lastRunDate)
        : isSet(object.last_run_date)
        ? globalThis.String(object.last_run_date)
        : "",
      lastRunStart: isSet(object.lastRunStart)
        ? globalThis.String(object.lastRunStart)
        : isSet(object.last_run_start)
        ? globalThis.String(object.last_run_start)
        : "",
      lastRunEnd: isSet(object.lastRunEnd)
        ? globalThis.String(object.lastRunEnd)
        : isSet(object.last_run_end)
        ? globalThis.String(object.last_run_end)
        : "",
    };
  },

  toJSON(message: ExchangeScheduleRow): unknown {
    const obj: any = {};
    if (message.exchange !== "") {
      obj.exchange = message.exchange;
    }
    if (message.symbol !== "") {
      obj.symbol = message.symbol;
    }
    if (message.region !== "") {
      obj.region = message.region;
    }
    if (message.runTimeEt !== "") {
      obj.runTimeEt = message.runTimeEt;
    }
    if (message.runTimeUtc !== "") {
      obj.runTimeUtc = message.runTimeUtc;
    }
    if (message.localClose !== "") {
      obj.localClose = message.localClose;
    }
    if (message.localTz !== "") {
      obj.localTz = message.localTz;
    }
    if (message.timeRemainingSeconds !== 0) {
      obj.timeRemainingSeconds = Math.round(message.timeRemainingSeconds);
    }
    if (message.lastRunDate !== "") {
      obj.lastRunDate = message.lastRunDate;
    }
    if (message.lastRunStart !== "") {
      obj.lastRunStart = message.lastRunStart;
    }
    if (message.lastRunEnd !== "") {
      obj.lastRunEnd = message.lastRunEnd;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExchangeScheduleRow>, I>>(base?: I): ExchangeScheduleRow {
    return ExchangeScheduleRow.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExchangeScheduleRow>, I>>(object: I): ExchangeScheduleRow {
    const message = createBaseExchangeScheduleRow();
    message.exchange = object.exchange ?? "";
    message.symbol = object.symbol ?? "";
    message.region = object.region ?? "";
    message.runTimeEt = object.runTimeEt ?? "";
    message.runTimeUtc = object.runTimeUtc ?? "";
    message.localClose = object.localClose ?? "";
    message.localTz = object.localTz ?? "";
    message.timeRemainingSeconds = object.timeRemainingSeconds ?? 0;
    message.lastRunDate = object.lastRunDate ?? "";
    message.lastRunStart = object.lastRunStart ?? "";
    message.lastRunEnd = object.lastRunEnd ?? "";
    return message;
  },
};

function createBaseGetExchangeScheduleResponse(): GetExchangeScheduleResponse {
  return { rows: [] };
}

export const GetExchangeScheduleResponse: MessageFns<GetExchangeScheduleResponse> = {
  encode(message: GetExchangeScheduleResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.rows) {
      ExchangeScheduleRow.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetExchangeScheduleResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetExchangeScheduleResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rows.push(ExchangeScheduleRow.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetExchangeScheduleResponse {
    return {
      rows: globalThis.Array.isArray(object?.rows) ? object.rows.map((e: any) => ExchangeScheduleRow.fromJSON(e)) : [],
    };
  },

  toJSON(message: GetExchangeScheduleResponse): unknown {
    const obj: any = {};
    if (message.rows?.length) {
      obj.rows = message.rows.map((e) => ExchangeScheduleRow.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetExchangeScheduleResponse>, I>>(base?: I): GetExchangeScheduleResponse {
    return GetExchangeScheduleResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetExchangeScheduleResponse>, I>>(object: I): GetExchangeScheduleResponse {
    const message = createBaseGetExchangeScheduleResponse();
    message.rows = object.rows?.map((e) => ExchangeScheduleRow.fromPartial(e)) || [];
    return message;
  },
};

function createBaseStartSchedulerRequest(): StartSchedulerRequest {
  return {};
}

export const StartSchedulerRequest: MessageFns<StartSchedulerRequest> = {
  encode(_: StartSchedulerRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartSchedulerRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartSchedulerRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): StartSchedulerRequest {
    return {};
  },

  toJSON(_: StartSchedulerRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<StartSchedulerRequest>, I>>(base?: I): StartSchedulerRequest {
    return StartSchedulerRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartSchedulerRequest>, I>>(_: I): StartSchedulerRequest {
    const message = createBaseStartSchedulerRequest();
    return message;
  },
};

function createBaseStartSchedulerResponse(): StartSchedulerResponse {
  return { success: false, message: "" };
}

export const StartSchedulerResponse: MessageFns<StartSchedulerResponse> = {
  encode(message: StartSchedulerResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartSchedulerResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartSchedulerResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartSchedulerResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: StartSchedulerResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StartSchedulerResponse>, I>>(base?: I): StartSchedulerResponse {
    return StartSchedulerResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartSchedulerResponse>, I>>(object: I): StartSchedulerResponse {
    const message = createBaseStartSchedulerResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseStopSchedulerRequest(): StopSchedulerRequest {
  return {};
}

export const StopSchedulerRequest: MessageFns<StopSchedulerRequest> = {
  encode(_: StopSchedulerRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StopSchedulerRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStopSchedulerRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): StopSchedulerRequest {
    return {};
  },

  toJSON(_: StopSchedulerRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<StopSchedulerRequest>, I>>(base?: I): StopSchedulerRequest {
    return StopSchedulerRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StopSchedulerRequest>, I>>(_: I): StopSchedulerRequest {
    const message = createBaseStopSchedulerRequest();
    return message;
  },
};

function createBaseStopSchedulerResponse(): StopSchedulerResponse {
  return { success: false, message: "" };
}

export const StopSchedulerResponse: MessageFns<StopSchedulerResponse> = {
  encode(message: StopSchedulerResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StopSchedulerResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStopSchedulerResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StopSchedulerResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: StopSchedulerResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StopSchedulerResponse>, I>>(base?: I): StopSchedulerResponse {
    return StopSchedulerResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StopSchedulerResponse>, I>>(object: I): StopSchedulerResponse {
    const message = createBaseStopSchedulerResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseRunExchangeJobRequest(): RunExchangeJobRequest {
  return { exchange: "", timeframe: "" };
}

export const RunExchangeJobRequest: MessageFns<RunExchangeJobRequest> = {
  encode(message: RunExchangeJobRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exchange !== "") {
      writer.uint32(10).string(message.exchange);
    }
    if (message.timeframe !== "") {
      writer.uint32(18).string(message.timeframe);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RunExchangeJobRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRunExchangeJobRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.exchange = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timeframe = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RunExchangeJobRequest {
    return {
      exchange: isSet(object.exchange) ? globalThis.String(object.exchange) : "",
      timeframe: isSet(object.timeframe) ? globalThis.String(object.timeframe) : "",
    };
  },

  toJSON(message: RunExchangeJobRequest): unknown {
    const obj: any = {};
    if (message.exchange !== "") {
      obj.exchange = message.exchange;
    }
    if (message.timeframe !== "") {
      obj.timeframe = message.timeframe;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RunExchangeJobRequest>, I>>(base?: I): RunExchangeJobRequest {
    return RunExchangeJobRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RunExchangeJobRequest>, I>>(object: I): RunExchangeJobRequest {
    const message = createBaseRunExchangeJobRequest();
    message.exchange = object.exchange ?? "";
    message.timeframe = object.timeframe ?? "";
    return message;
  },
};

function createBaseRunExchangeJobResponse(): RunExchangeJobResponse {
  return { success: false, message: "" };
}

export const RunExchangeJobResponse: MessageFns<RunExchangeJobResponse> = {
  encode(message: RunExchangeJobResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RunExchangeJobResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRunExchangeJobResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RunExchangeJobResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: RunExchangeJobResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RunExchangeJobResponse>, I>>(base?: I): RunExchangeJobResponse {
    return RunExchangeJobResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RunExchangeJobResponse>, I>>(object: I): RunExchangeJobResponse {
    const message = createBaseRunExchangeJobResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseListQueueTasksRequest(): ListQueueTasksRequest {
  return { queue: "" };
}

export const ListQueueTasksRequest: MessageFns<ListQueueTasksRequest> = {
  encode(message: ListQueueTasksRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.queue !== "") {
      writer.uint32(10).string(message.queue);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListQueueTasksRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListQueueTasksRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.queue = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListQueueTasksRequest {
    return { queue: isSet(object.queue) ? globalThis.String(object.queue) : "" };
  },

  toJSON(message: ListQueueTasksRequest): unknown {
    const obj: any = {};
    if (message.queue !== "") {
      obj.queue = message.queue;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListQueueTasksRequest>, I>>(base?: I): ListQueueTasksRequest {
    return ListQueueTasksRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListQueueTasksRequest>, I>>(object: I): ListQueueTasksRequest {
    const message = createBaseListQueueTasksRequest();
    message.queue = object.queue ?? "";
    return message;
  },
};

function createBaseQueueTask(): QueueTask {
  return { state: "", type: "", exchange: "", timeframe: "", nextProcessAt: undefined, id: "" };
}

export const QueueTask: MessageFns<QueueTask> = {
  encode(message: QueueTask, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== "") {
      writer.uint32(10).string(message.state);
    }
    if (message.type !== "") {
      writer.uint32(18).string(message.type);
    }
    if (message.exchange !== "") {
      writer.uint32(26).string(message.exchange);
    }
    if (message.timeframe !== "") {
      writer.uint32(34).string(message.timeframe);
    }
    if (message.nextProcessAt !== undefined) {
      Timestamp.encode(toTimestamp(message.nextProcessAt), writer.uint32(42).fork()).join();
    }
    if (message.id !== "") {
      writer.uint32(50).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueueTask {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueueTask();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.state = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.exchange = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.timeframe = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.nextProcessAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueueTask {
    return {
      state: isSet(object.state) ? globalThis.String(object.state) : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      exchange: isSet(object.exchange) ? globalThis.String(object.exchange) : "",
      timeframe: isSet(object.timeframe) ? globalThis.String(object.timeframe) : "",
      nextProcessAt: isSet(object.nextProcessAt)
        ? fromJsonTimestamp(object.nextProcessAt)
        : isSet(object.next_process_at)
        ? fromJsonTimestamp(object.next_process_at)
        : undefined,
      id: isSet(object.id) ? globalThis.String(object.id) : "",
    };
  },

  toJSON(message: QueueTask): unknown {
    const obj: any = {};
    if (message.state !== "") {
      obj.state = message.state;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.exchange !== "") {
      obj.exchange = message.exchange;
    }
    if (message.timeframe !== "") {
      obj.timeframe = message.timeframe;
    }
    if (message.nextProcessAt !== undefined) {
      obj.nextProcessAt = message.nextProcessAt.toISOString();
    }
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueueTask>, I>>(base?: I): QueueTask {
    return QueueTask.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueueTask>, I>>(object: I): QueueTask {
    const message = createBaseQueueTask();
    message.state = object.state ?? "";
    message.type = object.type ?? "";
    message.exchange = object.exchange ?? "";
    message.timeframe = object.timeframe ?? "";
    message.nextProcessAt = object.nextProcessAt ?? undefined;
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseListQueueTasksResponse(): ListQueueTasksResponse {
  return { tasks: [] };
}

export const ListQueueTasksResponse: MessageFns<ListQueueTasksResponse> = {
  encode(message: ListQueueTasksResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.tasks) {
      QueueTask.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListQueueTasksResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListQueueTasksResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tasks.push(QueueTask.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListQueueTasksResponse {
    return {
      tasks: globalThis.Array.isArray(object?.tasks) ? object.tasks.map((e: any) => QueueTask.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListQueueTasksResponse): unknown {
    const obj: any = {};
    if (message.tasks?.length) {
      obj.tasks = message.tasks.map((e) => QueueTask.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListQueueTasksResponse>, I>>(base?: I): ListQueueTasksResponse {
    return ListQueueTasksResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListQueueTasksResponse>, I>>(object: I): ListQueueTasksResponse {
    const message = createBaseListQueueTasksResponse();
    message.tasks = object.tasks?.map((e) => QueueTask.fromPartial(e)) || [];
    return message;
  },
};

/** SchedulerService provides scheduler monitoring and control. */
export type SchedulerServiceDefinition = typeof SchedulerServiceDefinition;
export const SchedulerServiceDefinition = {
  name: "SchedulerService",
  fullName: "stockalert.scheduler.v1.SchedulerService",
  methods: {
    getSchedulerStatus: {
      name: "GetSchedulerStatus",
      requestType: GetSchedulerStatusRequest,
      requestStream: false,
      responseType: GetSchedulerStatusResponse,
      responseStream: false,
      options: {},
    },
    getExchangeSchedule: {
      name: "GetExchangeSchedule",
      requestType: GetExchangeScheduleRequest,
      requestStream: false,
      responseType: GetExchangeScheduleResponse,
      responseStream: false,
      options: {},
    },
    startScheduler: {
      name: "StartScheduler",
      requestType: StartSchedulerRequest,
      requestStream: false,
      responseType: StartSchedulerResponse,
      responseStream: false,
      options: {},
    },
    stopScheduler: {
      name: "StopScheduler",
      requestType: StopSchedulerRequest,
      requestStream: false,
      responseType: StopSchedulerResponse,
      responseStream: false,
      options: {},
    },
    runExchangeJob: {
      name: "RunExchangeJob",
      requestType: RunExchangeJobRequest,
      requestStream: false,
      responseType: RunExchangeJobResponse,
      responseStream: false,
      options: {},
    },
    listQueueTasks: {
      name: "ListQueueTasks",
      requestType: ListQueueTasksRequest,
      requestStream: false,
      responseType: ListQueueTasksResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface SchedulerServiceImplementation<CallContextExt = {}> {
  getSchedulerStatus(
    request: GetSchedulerStatusRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GetSchedulerStatusResponse>>;
  getExchangeSchedule(
    request: GetExchangeScheduleRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GetExchangeScheduleResponse>>;
  startScheduler(
    request: StartSchedulerRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<StartSchedulerResponse>>;
  stopScheduler(
    request: StopSchedulerRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<StopSchedulerResponse>>;
  runExchangeJob(
    request: RunExchangeJobRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<RunExchangeJobResponse>>;
  listQueueTasks(
    request: ListQueueTasksRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ListQueueTasksResponse>>;
}

export interface SchedulerServiceClient<CallOptionsExt = {}> {
  getSchedulerStatus(
    request: DeepPartial<GetSchedulerStatusRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GetSchedulerStatusResponse>;
  getExchangeSchedule(
    request: DeepPartial<GetExchangeScheduleRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GetExchangeScheduleResponse>;
  startScheduler(
    request: DeepPartial<StartSchedulerRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<StartSchedulerResponse>;
  stopScheduler(
    request: DeepPartial<StopSchedulerRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<StopSchedulerResponse>;
  runExchangeJob(
    request: DeepPartial<RunExchangeJobRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<RunExchangeJobResponse>;
  listQueueTasks(
    request: DeepPartial<ListQueueTasksRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ListQueueTasksResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
