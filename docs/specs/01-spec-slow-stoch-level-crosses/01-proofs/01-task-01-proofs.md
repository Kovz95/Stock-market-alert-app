# Proofs: Task 01 — Extend `SlowStochConditionType` and expression generation in `types.ts`

## Planned evidence

- Diff of `apps/ui/web/app/alerts/add/_components/types.ts` showing the 8 new type literals added to `SlowStochConditionType`.
- Diff showing the 8 new `case` blocks added inside `case "slow_stoch":` in `buildConditionExpression`.
- Output of `pnpm typecheck` (exit 0, no errors).

## Completion notes

(Fill in after implementation)
