# Proofs: Task 02 — Add entries to `SLOW_STOCH_TYPES` and level inputs in `ConditionBuilder.tsx`

## Planned evidence

- Diff of `apps/ui/web/app/alerts/add/_components/ConditionBuilder.tsx` showing:
  - 8 new entries appended to `SLOW_STOCH_TYPES` with `group: "Level Crosses"`.
  - New conditional render block for `slowStochOversoldLevel` and `slowStochOverboughtLevel` inputs.
- Screenshot or description confirming the "Level Crosses" group appears in the dropdown.
- Confirmation that the level inputs do not appear when a non-cross type is selected.

## Completion notes

(Fill in after implementation)
