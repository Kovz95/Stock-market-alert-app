# Proofs: Task 03 — Add 8 catalog entries to `expr/catalog.go`

## Planned evidence

- Diff of `expr/catalog.go` showing the 8 new `case` blocks in `ExpandCatalogCondition`.
- Output of `go build ./expr/...` (exit 0).

## Completion notes

(Fill in after implementation)
