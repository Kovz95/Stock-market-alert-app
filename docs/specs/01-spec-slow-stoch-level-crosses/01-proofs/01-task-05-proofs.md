# Proofs: Task 05 — Full validation run

## Planned evidence

- Output of `go test ./expr/... ./indicator/...` (all packages `ok`, exit 0).
- Output of `pnpm typecheck` in `apps/ui/web` (exit 0).
- Output of `pnpm build` in `apps/ui/web` (exit 0).
- Confirmation all 7 acceptance criteria boxes ticked in `01-validation-slow-stoch-level-crosses.md`.

## Completion notes

(Fill in after implementation)
