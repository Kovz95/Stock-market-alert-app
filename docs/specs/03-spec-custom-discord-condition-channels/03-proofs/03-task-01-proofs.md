# Proofs: Task 01 - Extend the Discord proto with custom-channel RPCs

## Planned evidence

- Diff of `proto/discord/v1/discord.proto` showing the five new RPCs and all new messages.
- Output of the protobuf codegen command (`buf generate` or project equivalent) with no errors.
- `grep` output confirming new symbols exist in `gen/ts/discord/v1/discord.ts` and `gen/go/discord/v1/discord_grpc.pb.go`.

## Completion notes

(Fill in after implementation)
